---
import Layout from '../../layouts/Layout.astro';

// Metadatos de la página
export const frontmatter = {
  title: 'LandingPage Wizard',
  description: `Como un emprendedor que busca establecer una presencia en línea, quiero una herramienta que me ayude a crear una landing page efectiva sin necesidad de conocimientos técnicos, para poder captar leads y mostrar mi producto de forma atractiva. La herramienta debe ofrecer plantillas personalizables basadas en mi industria, sugerencias de contenido optimizadas para conversiones y una vista previa en tiempo real de cómo se verá la página en distintos dispositivos.`,
  tags: ['Landing Page', 'Creación de Páginas', 'Marketing Digital', 'Emprendimiento', 'Optimización de Conversiones']
};
---

<Layout title={frontmatter.title}>
  <a href={import.meta.env.BASE_URL} class="back-link">&larr; Volver a la Fábrica</a>
  
  <div class="tool-container">
    <h1>{frontmatter.title}</h1>
    <p>{frontmatter.description}</p>
    
    <!-- === COMIENZA EL CÓDIGO HTML DE LA HERRAMIENTA === -->
    <div class="tool-wrapper">
      <div class="form-container">
        <div class="input-group">
          <label for="template-select">Selecciona una Plantilla:</label>
          <select id="template-select" class="input-field">
            <option value="ecommerce">E-commerce</option>
            <option value="services">Servicios</option>
            <option value="education">Educación</option>
          </select>
        </div>

        <div class="input-group">
          <label for="headline-input">Encabezado:</label>
          <input type="text" id="headline-input" class="input-field" placeholder="Escribe tu encabezado aquí">
        </div>

        <div class="input-group">
          <label for="description-input">Descripción:</label>
          <textarea id="description-input" class="input-field" placeholder="Escribe una descripción aquí"></textarea>
        </div>

        <button id="generate-preview" class="btn btn-primary">Generar Vista Previa</button>
      </div>

      <div class="results-container">
        <div class="summary">
          <h4>Vista Previa</h4>
          <div id="preview" class="preview-content"></div>
        </div>
      </div>
    </div>
    <!-- === TERMINA EL CÓDIGO HTML DE LA HERRAMIENTA === -->
  </div>
</Layout>

<script>
// === COMIENZA EL CÓDIGO JAVASCRIPT DE LA HERRAMIENTA ===
const generatePreviewButton = document.getElementById('generate-preview');
const previewContainer = document.getElementById('preview');

generatePreviewButton.addEventListener('click', () => {
  const selectedTemplate = document.getElementById('template-select').value;
  const headline = document.getElementById('headline-input').value;
  const description = document.getElementById('description-input').value;

  // Generar contenido de vista previa
  let templateHTML = `<div class="preview-template ${selectedTemplate}">`;
  templateHTML += `<h2>${headline}</h2>`;
  templateHTML += `<p>${description}</p>`;
  templateHTML += `</div>`;

  // Actualizar el contenedor de vista previa
  previewContainer.innerHTML = templateHTML;
});
// === TERMINA EL CÓDIGO JAVASCRIPT DE LA HERRAMIENTA ===
</script>